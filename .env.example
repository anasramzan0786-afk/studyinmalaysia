# Copy this file to .env and fill in your values
# cp .env.example .env

# ─── Database ────────────────────────────────────────────────────────────────
# Local SQLite (default — zero setup for development)
DATABASE_URL="file:./prisma/dev.db"

# PostgreSQL (for production — e.g. Supabase, Neon, Railway, PlanetScale)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require"

# ─── Public env vars (safe to expose in browser) ─────────────────────────────
NEXT_PUBLIC_WHATSAPP_NUMBER="+601XXXXXXXXX"
NEXT_PUBLIC_SITE_URL="https://yourdomain.com"
NEXT_PUBLIC_SITE_NAME="Study Malaysia Portal"
